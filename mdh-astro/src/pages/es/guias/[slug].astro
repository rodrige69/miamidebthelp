---
import { getCollection, render } from 'astro:content';
import ArticleLayout from '../../../layouts/ArticleLayout.astro';

export async function getStaticPaths() {
  const articles = await getCollection('articles', ({ id }) => id.startsWith('es/'));
  return articles.map((article) => ({
    params: { slug: article.id.replace('es/', '').replace(/\.mdx$/, '') },
    props: { article },
  }));
}

const { article } = Astro.props;
const { Content } = await render(article);
const data = article.data;

const enArticles = await getCollection('articles', ({ id }) => id.startsWith('en/'));
const enMatch = enArticles.find((a) => a.data.translationKey === data.translationKey);
const altLangPath = enMatch ? `/guides/${enMatch.id.replace('en/', '').replace(/\.mdx$/, '')}/` : undefined;
---

<ArticleLayout
  title={data.title}
  metaDescription={data.metaDescription}
  lang="es"
  altLangPath={altLangPath}
  category={data.category}
  datePublished={data.datePublished}
  dateModified={data.dateModified}
  reviewedBy={data.reviewedBy}
  readTime={data.readTime}
  sources={data.sources}
  directAnswer={data.metaDescription}
  breadcrumbLabel={data.title}
  relatedCategorySlug={data.relatedCategorySlug}
  whatChanged={data.whatChanged}
>
  <Content />
</ArticleLayout>
